Static HTML build generated from the PHP source.

Key points:
- All page files have been rendered to .html equivalents.
- Source-only folders such as includes/ and scripts/ were not included.
- assets/, styles.css, robots.txt, sitemap.xml, and content/ were copied through.
- For Cloudflare Pages, set Root directory to the folder you deploy and leave Build command blank.
